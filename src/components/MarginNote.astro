---
/**
 * A note in the margin on wide screens; inline, set off by a rule, on small
 * ones. Requires the parent prose block to carry `prose--with-margins`.
 */
---

<aside class="marginnote">
  <slot />
</aside>
